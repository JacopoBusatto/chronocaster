**TO FIX IN THE WEB APP**
- the zero of the surface of the plot should be moved slightly lower to see the full picture on the surface
- the "next phase" button should be moving with the scroll of the app, maybe floating on the bottom right corner
- in the "required speed per station table" the speed parameeter is too much on the right and when the plot updates, the screen get reset and it is not possible to check the values. I suggest to remove the column "type" and "route"
- when we correct the depth, after the profile (U-stations), the filtering depth marker updates. However the blue line (predicted) an then the grey line + the marker, follow the old setting, without updating. I think they should follow the new updated path.
- the fact that the image reset every 2 seconds makes impossible to scroll left any object: tables, list of phases (the one that has crossed out the phases already completed) are set back at the original position every 2 seconds.
